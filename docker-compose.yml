services:
  local-clipboard:
    build: .
    environment:
      - PORT=${PORT:-8080}
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    volumes:
      - clipboard-data:/data
    restart: unless-stopped

volumes:
  clipboard-data:
